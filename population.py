import numpy as np
import car

class Population:

    def __init__(self, startx, starty, size):
        self.size = size

        #self.cars = [car.Car(np.random.uniform(0, 1000), np.random.uniform(0, 900)) for i in range(0, size)]



